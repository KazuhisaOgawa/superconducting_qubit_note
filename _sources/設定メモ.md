# 個人用アクセストークン

右上のプロフィールアイコン
→Settings
→Developer settings
→Personal access tokens
→Tokens (classic)

これをtoken3.txtに貼り付ける

# 操作方法
jb build ./
でノートブックがビルドされる。
ghp-import -n -p -f _build/html
で公開ノートブックの更新。